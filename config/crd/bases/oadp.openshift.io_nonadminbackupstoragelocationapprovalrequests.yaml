---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: nonadminbackupstoragelocationapprovalrequests.oadp.openshift.io
spec:
  group: oadp.openshift.io
  names:
    kind: NonAdminBackupStorageLocationApprovalRequest
    listKind: NonAdminBackupStorageLocationApprovalRequestList
    plural: nonadminbackupstoragelocationapprovalrequests
    shortNames:
    - nabslapprovalrequest
    singular: nonadminbackupstoragelocationapprovalrequest
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NonAdminBackupStorageLocationApprovalRequest is the Schema for
          the nonadminbackupstoragelocationapprovalrequests API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NonAdminBackupStorageLocationApprovalRequestSpec defines
              the desired state of NonAdminBackupStorageLocationApprovalRequest
            properties:
              pendingApprovalDecision:
                description: |-
                  pendingApprovalDecision is the decision of the cluster admin on the currently pending NonAdminBackupStorageLocation creation or update.
                  This applies only to the pending NonAdminBackupStorageLocation Spec waiting for the approval.
                  If set to "reject", the update request will not be approved, but an already approved spec remains unless explicitly revoked.
                enum:
                - approve
                - reject
                - pending
                type: string
              revokeApprovedSpec:
                description: |-
                  revokeApprovedSpec indicates whether the previously approved NonAdminBackupStorageLocation Spec should be revoked.
                  if set to true, the entire BSL will be removed regardless of the PendingApprovalDecision and the PendingApprovalDecision will become "pending".
                  The AprovedSpec from the Status will be updated to the PendingSpec.
                type: boolean
            type: object
          status:
            description: NonAdminBackupStorageLocationApprovalRequestStatus defines
              the observed state of NonAdminBackupStorageLocationApprovalRequest
            properties:
              veleroBackupStorageLocationApprovalRequest:
                description: VeleroBackupStorageLocationApprovalRequest contains information
                  of the related Velero backup object.
                properties:
                  approvedSpec:
                    description: approvedSpec contains the approved spec request for
                      the NonAdminBackupStorageLocation
                    properties:
                      accessMode:
                        description: AccessMode defines the permissions for the backup
                          storage location.
                        enum:
                        - ReadOnly
                        - ReadWrite
                        type: string
                      backupSyncPeriod:
                        description: BackupSyncPeriod defines how frequently to sync
                          backup API objects from object storage. A value of 0 disables
                          sync.
                        nullable: true
                        type: string
                      config:
                        additionalProperties:
                          type: string
                        description: Config is for provider-specific configuration
                          fields.
                        type: object
                      credential:
                        description: Credential contains the credential information
                          intended to be used with this location
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              TODO: Add other useful fields. apiVersion, kind, uid?
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              TODO: Drop `kubebuilder:default` when controller-gen doesn't need it https://github.com/kubernetes-sigs/kubebuilder/issues/3896.
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      default:
                        description: Default indicates this location is the default
                          backup storage location.
                        type: boolean
                      objectStorage:
                        description: ObjectStorageLocation specifies the settings
                          necessary to connect to a provider's object storage.
                        properties:
                          bucket:
                            description: Bucket is the bucket to use for object storage.
                            type: string
                          caCert:
                            description: CACert defines a CA bundle to use when verifying
                              TLS connections to the provider.
                            format: byte
                            type: string
                          prefix:
                            description: Prefix is the path inside a bucket to use
                              for Velero storage. Optional.
                            type: string
                        required:
                        - bucket
                        type: object
                      provider:
                        description: Provider is the provider of the backup storage.
                        type: string
                      validationFrequency:
                        description: ValidationFrequency defines how frequently to
                          validate the corresponding object storage. A value of 0
                          disables validation.
                        nullable: true
                        type: string
                    required:
                    - objectStorage
                    - provider
                    type: object
                  nacuuid:
                    description: nacuuid references the NonAdminBackupStorageLocation
                      object by it's label containing same NACUUID.
                    type: string
                  name:
                    description: name references the NonAdminBackupStorageLocation
                      object by it's name.
                    type: string
                  namespace:
                    description: namespace references the Namespace in which NonAdminBackupStorageLocation
                      exists.
                    type: string
                  pendingApprovalSpec:
                    description: pendingApprovalSpec contains the pending spec request
                      for the NonAdminBackupStorageLocation
                    properties:
                      accessMode:
                        description: AccessMode defines the permissions for the backup
                          storage location.
                        enum:
                        - ReadOnly
                        - ReadWrite
                        type: string
                      backupSyncPeriod:
                        description: BackupSyncPeriod defines how frequently to sync
                          backup API objects from object storage. A value of 0 disables
                          sync.
                        nullable: true
                        type: string
                      config:
                        additionalProperties:
                          type: string
                        description: Config is for provider-specific configuration
                          fields.
                        type: object
                      credential:
                        description: Credential contains the credential information
                          intended to be used with this location
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              TODO: Add other useful fields. apiVersion, kind, uid?
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              TODO: Drop `kubebuilder:default` when controller-gen doesn't need it https://github.com/kubernetes-sigs/kubebuilder/issues/3896.
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      default:
                        description: Default indicates this location is the default
                          backup storage location.
                        type: boolean
                      objectStorage:
                        description: ObjectStorageLocation specifies the settings
                          necessary to connect to a provider's object storage.
                        properties:
                          bucket:
                            description: Bucket is the bucket to use for object storage.
                            type: string
                          caCert:
                            description: CACert defines a CA bundle to use when verifying
                              TLS connections to the provider.
                            format: byte
                            type: string
                          prefix:
                            description: Prefix is the path inside a bucket to use
                              for Velero storage. Optional.
                            type: string
                        required:
                        - bucket
                        type: object
                      provider:
                        description: Provider is the provider of the backup storage.
                        type: string
                      validationFrequency:
                        description: ValidationFrequency defines how frequently to
                          validate the corresponding object storage. A value of 0
                          disables validation.
                        nullable: true
                        type: string
                    required:
                    - objectStorage
                    - provider
                    type: object
                required:
                - approvedSpec
                - pendingApprovalSpec
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
